# Core tracking
opencv-python
numpy
ultralytics

# Camera
# pyrealsense2 has prebuilt pip wheels for Windows and Linux x86_64/aarch64 only —
# there is no macOS wheel at all (Intel dropped official macOS builds), and Jetson's
# JetPack Python is too old for the wheel's ABI. On both Mac and Jetson, build
# librealsense + the Python bindings from source instead (see SETUP.md) and skip
# this line's pip install.
pyrealsense2 ; platform_system != "Darwin"

# Web UI (server.py)
fastapi
uvicorn[standard]
jinja2
